function calcDifference() {
  const zahlOne = 27;
  let zahlVar = 300;

  let difference = zahlVar - zahlOne;

  if (difference >= 27) {
    difference = difference * 2;
    console.log(difference);
  } else {
    console.log(difference);
  }
}

calcDifference();
